#include <stdio.h>
#include <stdlib.h>

int Circle(int x, int y, int z)
{
	printf("Circle (%d, %d, %d)\n",x, y, z);
}

main()
{
	int n, *a_x, *a_y, *a_z;
	printf("Enter the number of circles ");
	scanf("%d",&n);

	
	
	return 0;
}

